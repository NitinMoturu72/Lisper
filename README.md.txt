Lisper – Random Secret Card App 🎴
Lisper is a fun and visually appealing web application that displays random user-generated secrets with a beautiful animated card interface. It fetches secrets from a public API and displays them dynamically using Express.js, EJS, and Axios, all styled with glowing animated effects using CSS.

✨ Features
🎭 Fetches and displays random secrets from an external API.

📃 Renders data dynamically using EJS templating.

🎨 Beautiful CSS animations and gradients using custom properties and @property.

💡 Clean and responsive UI built with pure HTML/CSS.

📦 Express server with Axios integration for easy backend communication.

A rotating gradient card reveals a random secret when hovered over, with the username shown below.

🚀 Getting Started

1. Clone the repository

bash
git clone https://github.com/yourusername/lisper.git
cd lisper

2. Install dependencies

bash
npm install

3. Start the server

bash
node index.js

Then, visit http://localhost:3000 in your browser.

Ensure you have Node.js installed and an internet connection (for fetching from the secrets API).


🛠 Technologies Used
Node.js

Express.js

EJS

Axios

HTML/CSS

Google Fonts

CSS Custom Properties & Animations